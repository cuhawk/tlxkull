---
description: Run the wiki health check. No arguments.
---

# /wiki-lint

Invoke the `wiki-lint` skill. Writes `wiki/_lint_<YYYYMMDD>.md` and
surfaces a one-screen summary (orphans / dangling links /
contradictions / stale entries).
